#!/usr/bin/env bash
# Generated by shakacode/agent-workflows bin/push-downstream.
# Build production assets through the repository's full Rails-aware build.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec bin/build-production "$@"
