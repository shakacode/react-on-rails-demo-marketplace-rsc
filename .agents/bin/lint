#!/usr/bin/env bash
# Generated by shakacode/agent-workflows bin/push-downstream.
# Run the documented JavaScript/TypeScript and Ruby linters.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"

pnpm type-check
pnpm lint
exec bundle exec rubocop "$@"
