#!/usr/bin/env bash
# Generated by shakacode/agent-workflows bin/push-downstream.
# Run this app's Rails test task; it has no committed RSpec suite.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec bundle exec rake "$@"
