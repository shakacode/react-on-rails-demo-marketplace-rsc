#!/usr/bin/env bash
# Generated by shakacode/agent-workflows bin/push-downstream.
# Run this app's executable demo-fleet verification, including the public-route
# request specs, Rails task, production build, and RSC validation.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec script/demo-fleet-verify "$@"
