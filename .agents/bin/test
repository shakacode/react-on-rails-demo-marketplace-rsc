#!/usr/bin/env bash
# Generated by shakacode/agent-workflows bin/push-downstream.
# Run this app's executable demo-fleet verification. The repository has no
# committed unit or RSpec suite; this covers the Rails task and RSC validation.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec script/demo-fleet-verify "$@"
